This repository contains all the code required to generate the figures as seen in the final paper. The code is organized based on section further by what figure it generates, and additionally includes code for animitions. All of the code should theoretically be ready to run by either running directly as .py file or in a jupyter notebook, assuming necessary libraries (e.g. "numba") have been installed. 
